  <h1>List all tasks</h1>

  <% @tasks.each do |task| %>
  <h2><%= task.title %></h2>
  <p><%= task.description %></p>


  <%= link_to "Descricao", task_path(task) %>
  <%= link_to "Delete", task_path(task), method: :delete %>
  <%= link_to "Edit", edit_path(task) %>

  <% end %>
